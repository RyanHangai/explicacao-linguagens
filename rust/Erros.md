Valor literal (literal out of range):

é porque o valor que **não** vem de variavel, nem chamada de funcao, é o valor literal que esta no codigo.

Tipo divergentes (mismatched types):

É quando voce declara um valor que nao é do tipo certo